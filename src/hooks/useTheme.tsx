// import { useState } from "react";
// import { createTheme, ThemeOptions } from "../constants/theme";
const useTheme = (options = { mode: "light" }) => {
//     const defaultTheme = createTheme(options)
//   const [theme, setT] = useState(defaultTheme);
//   const setTheme = (options) => {
//     setT(createTheme(options));
//   };
//   return [theme, setTheme];
};

export default useTheme;
