import { render, cleanup, waitFor, screen } from '@testing-library/react'
import { App } from '../src'
describe('App test', () => {
  beforeEach(() => {})

  afterEach(cleanup)
  it('renders without crashing', async () => {})
})
